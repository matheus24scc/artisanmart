# Test-only dependencies for the ArtisanMart backend.
# Install with: pip install -r requirements-dev.txt
# (runtime deps live in requirements.txt)
pytest==9.1.1
# httpx 0.28+ removed the `app=` kwarg that starlette 0.26.1's TestClient relies on.
httpx==0.27.2
